---
phase: 01-research-and-prototyping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/reads.rs, src/fastx.rs]
autonomous: true
requirements: [SBC-01, SBC-02, SBC-05]
must_haves:
  truths:
    - "The `reads` subcommand can successfully parse SAM, BAM, and CRAM files."
    - "Read lengths are correctly gathered from SAM/BAM/CRAM records (using the sequence field)."
    - "Target base calculation correctly uses lengths gathered from SBC files."
  artifacts:
    - path: "src/reads.rs"
      provides: "Integration of SBC reading into the Runner implementation"
    - path: "src/fastx.rs"
      provides: "Abstractions or extensions to handle SAM/BAM/CRAM record lengths"
  key_links:
    - from: "src/reads.rs"
      to: "noodles"
      via: "importing noodles and noodles_util"
---

<objective>
Implement core SAM/BAM/CRAM (SBC) reading and length gathering in the `reads` subcommand.
This is the first step toward full SBC support, allowing the application to "see" the reads in these formats.

Purpose: To enable rasusa to process alignment formats in the same way it processes FASTA/FASTQ.
Output: Working length gathering for SAM/BAM/CRAM files.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/reads.rs
@src/fastx.rs
@src/alignment.rs

<interfaces>
From src/alignment.rs:
```rust
use noodles_util::alignment::{
    self,
    io::{indexed_reader, Format},
};
// Use noodles_util to create an auto-detecting reader
let mut reader = alignment::io::reader::Builder::default()
    .build_from_path(path)?;
let header = reader.read_header()?;
for result in reader.records(&header) {
    let record = result?;
    // record.sequence().len() provides the length
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SBC read lengths gathering</name>
  <files>src/reads.rs, src/fastx.rs</files>
  <action>
    Modify `src/reads.rs` and `src/fastx.rs` (or introduce a new abstraction) to support gathering read lengths from SAM/BAM/CRAM files.
    - Leverage `noodles_util::alignment::io::reader::Builder` for automatic format detection.
    - For each record in the SBC file, extract the sequence length.
    - If a record has no sequence, handle it gracefully (e.g., skip or use 0, but usually SBC for reads will have sequences).
    - Ensure `Reads::run` can handle SBC inputs when calling for read lengths.
  </action>
  <verify>
    <automated>cargo test --lib fastx::tests::get_read_lengths_for_fastq</automated>
  </verify>
  <done>SAM/BAM/CRAM files can be passed to the length gathering logic and return correct values.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure correct target base calculation</name>
  <files>src/reads.rs</files>
  <action>
    Verify that `target_total_bases` calculation in `src/reads.rs` correctly accounts for the lengths gathered from SBC files.
    - This should already work if the lengths are correctly gathered into the `read_lengths` vector.
    - Add a test case or log validation to ensure that the target bases are calculated based on SBC record lengths.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Target bases are correctly calculated for SBC inputs.</done>
</task>

</tasks>

<verification>
- Gather read lengths from a small SAM file.
- Gather read lengths from a small BAM file.
- Verify that the number of reads and total bases match expectations for these formats.
</verification>

<success_criteria>
- `rasusa reads` can process a BAM file for the "gathering read lengths" phase without error.
- Unit tests for SBC length gathering pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-research-and-prototyping/01-01-SUMMARY.md`
</output>
