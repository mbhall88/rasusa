---
phase: 01-research-and-prototyping
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [src/reads.rs, src/cli.rs]
autonomous: true
requirements: [SBC-04, SBC-06]
must_haves:
  truths:
    - "Single-file paired-end support for SAM/BAM/CRAM works as expected using the SEGMENTED flag."
    - "CLI correctly accepts SAM/BAM/CRAM files in the `reads` subcommand."
    - "Integration tests confirm that both reads of a pair in a single BAM are kept or discarded together."
  artifacts:
    - path: "src/reads.rs"
      provides: "PE logic for single-file SBC"
    - path: "src/cli.rs"
      provides: "Updated command-line parsing for new formats"
  key_links:
    - from: "src/reads.rs"
      to: "noodles::sam::alignment::record::Flags"
      via: "Using the is_segmented() flag for PE detection"
---

<objective>
Implement paired-end support for single SAM/BAM/CRAM files and update the CLI to officially support these formats.

Purpose: To complete the feature by ensuring PE data integrity and enabling user access via the CLI.
Output: Feature-complete support for SBC in the `reads` subcommand.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/reads.rs
@src/cli.rs
@.planning/phases/01-research-and-prototyping/01-02-SUMMARY.md

<interfaces>
From src/reads.rs:
```rust
// In existing paired logic:
// - R1 then R2 (input length 2)
// For single-file PE:
// - Read 1 (record) + Read 2 (consecutive record with same name and SEGMENTED flag)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Handle single-file paired-end data</name>
  <files>src/reads.rs</files>
  <action>
    Modify `src/reads.rs` to detect and handle paired-end data in a single SAM/BAM/CRAM file.
    - Check the `SEGMENTED` (0x1) flag in records.
    - If a file contains segmented records, assume they are paired and handle them together (e.g., matching by name or assuming they are consecutive).
    - If they are paired, their lengths should be added together during the first pass (similar to the current 2-file paired logic).
    - Ensure both segments of a pair are either kept or discarded.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Single-file SAM/BAM/CRAM PE data is correctly handled.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLI to accept SAM/BAM/CRAM extensions</name>
  <files>src/cli.rs, src/reads.rs</files>
  <action>
    Update the CLI definition for the `reads` subcommand to officially accept SAM/BAM/CRAM extensions.
    - Modify the help text to mention these formats.
    - Ensure that the path validation and extension parsing in `src/cli.rs` and `src/reads.rs` allow these formats.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>CLI correctly handles SBC extensions for the `reads` subcommand.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Full SAM/BAM/CRAM support in the `reads` subcommand.</what-built>
  <how-to-verify>
    1. Run `cargo run -- reads tests/cases/test.bam -c 1` and verify it works.
    2. Run `cargo run -- reads tests/cases/test.paired.bam -c 1` and verify that paired-end integrity is maintained (both R1 and R2 are kept).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- Verify with `tests/cases/test.bam` (single-end aligned).
- Verify with `tests/cases/test.paired.bam` (paired-end aligned).
- Ensure output format matches input.
</verification>

<success_criteria>
- Single-file BAM PE subsampling works correctly.
- CLI allows BAM inputs.
- All integration tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-research-and-prototyping/01-03-SUMMARY.md`
</output>
